# Core dependencies
Flask<2.3.0,>=2.0.0
connexion[swagger-ui]==2.14.2
python-dotenv==1.0.0
marshmallow==3.20.1
pydantic==2.4.2
flask-cors==4.0.0

# Printer communication
brother-ql-inventree==1.3
# Pillow processes user-supplied images. 10.0.1 is vulnerable to
# CVE-2023-50447 (arbitrary code execution via ImageMath.eval) and
# CVE-2024-28219 (buffer overflow in _imagingcms). 10.4.x fixes both and
# remains compatible with Python 3.9.
Pillow==10.4.0
pysnmp==4.4.12
# pysnmp 4.4.12 breaks against pyasn1>=0.5 (removed pyasn1.compat.octets);
# pin to a compatible release so the optional SNMP import does not crash.
pyasn1==0.4.8
qrcode==7.4.2
# PDF rendering. pypdfium2 bundles the PDFium binary in its wheel (no system
# deps), Apache-2.0/BSD licensed and compatible with Python 3.9.
pypdfium2==4.30.0

# Production WSGI server (py3.9-compatible). Run with --workers 1 so the
# keep-alive singleton/thread stays in a single process (see docker-entrypoint.sh).
gunicorn==21.2.0

# Logging and error handling
structlog==23.1.0

# Testing
pytest==7.4.2
pytest-cov==4.1.0
