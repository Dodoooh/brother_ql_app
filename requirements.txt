# Core dependencies
# connexion 2.14.2 requires werkzeug<2.3, which in turn pins Flask to 2.2.x.
# Both stay on those versions until the connexion 3 migration.
Flask<2.3.0,>=2.0.0
connexion[swagger-ui]==2.14.2
flask-cors==6.0.5

# Printer communication
brother-ql-inventree==1.3
Pillow==12.3.0
qrcode==7.4.2
# PDF rendering. pypdfium2 bundles the PDFium binary in its wheel (no system
# deps) and is Apache-2.0/BSD licensed.
pypdfium2==4.30.0

# Production WSGI server. Run with --workers 1 so the keep-alive
# singleton/thread stays in a single process (see docker-entrypoint.sh).
gunicorn==23.0.0

# Logging and error handling
structlog==23.1.0
