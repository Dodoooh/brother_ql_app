# Core dependencies
# The `flask` extra is not optional: without it connexion 3 refuses to build a
# Flask application at all ("Please install connexion using the 'flask' extra").
connexion[flask,swagger-ui]==3.3.0
Flask==3.1.3
flask-cors==6.0.5
# Declared explicitly although connexion already pulls it in: src/app.py imports
# yaml directly to read openapi.yaml before handing it to connexion. Relying on
# the transitive install would break that import silently the day connexion
# changes its own dependencies (the connexion 3 migration, most likely).
PyYAML==6.0.3

# Printer communication
brother-ql-inventree==1.3
Pillow==12.3.0
qrcode==8.2
# PDF rendering. pypdfium2 bundles the PDFium binary in its wheel (no system
# deps) and is Apache-2.0/BSD licensed.
pypdfium2==5.12.1

# Production server. Connexion 3 is ASGI, so gunicorn runs it through uvicorn's
# worker class; gunicorn still supplies the process supervision. Run with
# --workers 1 so the keep-alive, queue and relay singletons stay in one process
# (see docker-entrypoint.sh).
gunicorn==26.0.0
uvicorn==0.38.0

# Logging and error handling
structlog==26.1.0
